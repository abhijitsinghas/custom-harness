import 'package:flutter/material.dart';

/// Bulk scanner screen for scanning multiple books at once.
/// US-0.4.9: /bulk-scanner route placeholder.
class BulkScannerScreen extends StatelessWidget {
  const BulkScannerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bulk Scanner')),
      body: const Center(
        child: Text('Bulk Scanner — Placeholder'),
      ),
    );
  }
}
