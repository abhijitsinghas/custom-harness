import 'package:flutter/material.dart';

/// Share library screen for sharing with friends/family.
/// US-0.4.9: /share-library route placeholder.
class ShareLibraryScreen extends StatelessWidget {
  const ShareLibraryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Share Library')),
      body: const Center(
        child: Text('Share Library — Placeholder'),
      ),
    );
  }
}
